const firstName = prompt("Masukan Nama anda");

const user = {
   name: {
       first: firstName,
   },
};
alert("Terimakasih " + user.name.first + "  telah mengunjungi website kami!");




















